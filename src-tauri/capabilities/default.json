{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for lookup and manage windows",
  "windows": ["lookup", "manage"],
  "permissions": [
    "core:default",
    "autostart:default"
  ]
}
